import { GlobalConfigProps } from '@/types/global/aem-definition';

export type CustomerNumberParsedProps = {
  customerNumberTitle?: string;
  customerNumber?: string;
  globalConfig?: GlobalConfigProps;
};
