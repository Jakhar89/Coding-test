import { validIcons } from "@/utility/components/Icon/definitions";

export type GetInTouchProps = {
  title?: string;
  summary?: string;
  buttonLabel?: string;
  iconName?: validIcons;
  buttonPath?: string;
};
