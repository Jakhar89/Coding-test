import { GlobalConfigProps } from '@/types/global/aem-definition';

export type MarketingContentProps = {
  imagePickerMarketing?: string;
  imagePickerLifestyle: string;
  marketingImageUrl?: string;
  globalConfig?: GlobalConfigProps;
};
